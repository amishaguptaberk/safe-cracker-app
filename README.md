# Safe Cracker

A small Flask app that cracks a 10-digit combination lock digit-by-digit and updates progress in the browser via polling.

## Setup

```
pip install -r requirements.txt
python app.py
```

Open http://localhost:5001, enter any 10-digit combination, and watch the attempt counter climb in real time.

